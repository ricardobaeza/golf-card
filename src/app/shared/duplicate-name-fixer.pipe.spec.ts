import { DuplicateNameFixerPipe } from './duplicate-name-fixer.pipe';

describe('DuplicateNameFixerPipe', () => {
  it('create an instance', () => {
    const pipe = new DuplicateNameFixerPipe();
    expect(pipe).toBeTruthy();
  });
});
